# Questlog Frontend — copy this file to .env.local and fill in your values.

# URL of the Questlog API backend (Render service or local dev).
# In production (Vercel), set this as a Vercel environment variable.
# NEXT_PUBLIC_API_URL=https://questlog-api.onrender.com
